def swap(values_list):
    values_list[0], values_list[-1] = values_list[-1], values_list[0]
    return values_list 

values_list = input().split(',') # Program receives comma-separated values
swap(values_list)

print(values_list)
